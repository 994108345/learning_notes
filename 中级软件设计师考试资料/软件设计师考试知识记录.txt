数据类型
	浮点型：1.2345 × 10^2 ，其中 1.2345 为尾数，10 为基数，2 为指数(阶码)。尾数也叫做有效数字。
		两个浮点数相加，首先对阶，将小阶向大阶对齐，同时将尾数右移n位，n为阶差的绝对值

数据结构
	图：（有n个结点和e条边）
		分类
			有向图
			无向图
		遍历方式
			深度优先：	
			广度优先：
		存储结构
			邻接矩阵
				时间复杂度：n^2
			邻接表：
				时间复杂度：e

计算机基础技术
	中断
		返回继续执行原程序
	
	海明码（汉明码）
		
		计算校验位数
			2^r -1 >= k+r :k为数据信息长度，r为校验位长度，k+r是整个码字长度
		确定校验码位置
			2^(i-1) :i为第几位校验码。
		确定数据的位置
			除了校验码的位置，其他就都是数据的位置了，按顺序放置
		
		
		
	DRAM（Dynamic Random Access Memory），即动态随机存取存储器，最为常见的系统内存。
	SRAM：静态随机存取存储器（Static Random-Access Memory，SRAM）是随机存取存储器的一种。
			
	DMA模式：DMA的英文拼写是“Direct Memory Access”，汉语的意思就是直接内存访问，是一种不经过CPU而直接从内存存取数据的数据交换模式。

	
Cpu指令的流水执行
	cpu指令执行一般包括取指、译码和执行
	消耗时间注意，最慢的阶段的位置
		
VLIW是英文“Very Long Instruction Word”的缩写，中文意思是“超长指令集架构”			
			
			
			
			
			
			
			
			
			