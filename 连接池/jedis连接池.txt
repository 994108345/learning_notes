Jedis连接池
	目前可知：redis专用连接池
	
	
	
直连和连接池的优缺点
	直连的优点：简单方便，适合长期少量的连接场景。
	连接池的优点：无需每次连接都生成Jedis对象，降低开销；使用连接池的形式保护和控制资源的使用。
	
	直连的缺点：存在每次新建/关闭TCP连接的开销；资源无法控制，极端情况会出现连接泄露；jedis对象线程不安全。
	连接池的缺点：相对于直连，使用相对麻烦，尤其在资源的管理上需要很多参数来保证，一旦规划不合理也会出现问题。